{
    "name": "verisure-inline-funnel-pkg",
    "version": "0.0.9",
    "scripts": {
        "version": "git tag $npm_package_version && git push origin $npm_package_version &&  ./scripts/upload-github-release-asset.sh owner=adaptdk repo=verisure-inline-funnel-pkg tag=$npm_package_version filename=../build",
        "postversion": "git commit -m \"Bumping up version...\" package.json && git push"
    },
    "devDependencies": {
        "cross-var": "^1.1.0",
        "github-release-cli": "^1.3.1"
    },
    "dependencies": {}
}
