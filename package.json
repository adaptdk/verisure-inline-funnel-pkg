{
    "name": "verisure-inline-funnel-pkg",
    "version": "0.1.1",
    "scripts": {
        "version": "github-release upload --owner adaptdk --repo verisure-inline-funnel-pkg --tag $npm_package_version --name $npm_package_version build",
        "postversion": "git commit -m \"Bumping up version...\" package.json && git push"
    },
    "devDependencies": {
        "cross-var": "^1.1.0",
        "github-release-cli": "^1.3.1"
    },
    "dependencies": {}
}
